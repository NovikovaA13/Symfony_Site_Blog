{% extends 'base.html.twig' %}

{% block title %}Ajout d'une catégorie{% endblock %}

{% block body %}

    <h1 class="mt-3 mb-5 text-center">Ajout d'une catégorie</h1>
    <div>
        {{ form_start(form) }}
        {{ form_errors(form, {'attr': {'label': '', 'class': 'alert alert-danger'}}) }}
        {{ form_row(form.name) }}
        <button type="submit" class="btn btn-success">Ajouter</button>
        {{ form_end(form) }}
    </div>
    <div>

    </div>

{% endblock %}
